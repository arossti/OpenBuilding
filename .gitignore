.DS_Store
*.tgz
.obsidian
node_modules/
PDF-Parser/logs/

# Local staging of source data into PDF-Parser/data/ via `npm run stage:data`.
# Not committed — the Pages workflow does the same copy at deploy time.
PDF-Parser/data/schema/
PDF-Parser/data/beam/

# Large binary files (will be managed with DVC)
*.ifc
*.ifc.zip

# Documents — kept locally, not tracked in git
*.pdf
!PDF-Parser/sample.pdf
*.docx
*.xlsx
*.xls

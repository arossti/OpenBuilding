.DS_Store
*.tgz
.obsidian
node_modules/
logs/

# Playwright MCP working dir (console logs + snapshot YAMLs from agent-driven tests)
.playwright-mcp/

# Playwright verification screenshots at repo root (one-off, not documentation)
/epd-*.png
# EPD-Parser calibration JSON dumps (ad-hoc analysis from Playwright eval)
/epd-*.json

# Local working notes
BfCA Server Commands.txt

# Local staging of source data into data/ via `npm run stage:data`.
# Not committed — the Pages workflow does the same copy at deploy time.
data/schema/
data/beam/

# Large binary files (will be managed with DVC)
*.ifc
*.ifc.zip

# Documents — kept locally, not tracked in git
*.pdf
!docs/sample.pdf
*.docx
*.xlsx
*.xls
